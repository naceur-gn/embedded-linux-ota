# OTA Device Configuration
# This file defines the device identity for the OTA system.

# Unique identifier for this device
device_id=device-001

# Hardware revision
hardware_version=revA

# Current software version (semver: MAJOR.MINOR.PATCH)
software_version=1.0.0

# Active boot slot (A or B)
active_slot=A
